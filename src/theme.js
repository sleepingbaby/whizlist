import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#597B91",
    },
    secondary: {
      main: "#3E4D6A",
    },
  },
});

export default theme;
